# 個人設定

## Python
- パッケージマネージャーは `uv` を使う

## プロジェクト配置
- Mac・Windows共に `~/dev/` 内に新規プロジェクトフォルダを作成して配置する

## Deluge 改行処理
- `\n` ではなく `hextoText("0A")` を使う
- 例: `newline = hextoText("0A"); text = "1行目" + newline + "2行目";`

## 回答スタイル
- 技術情報・料金・仕様などは必ず最新情報を確認してから答える
- ユーザーは非エンジニアのため、専門用語を使う際は簡単な説明を添える
